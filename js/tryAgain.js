const tryAgain = document.getElementById('btn-close');

tryAgain.addEventListener('click', () => {
    // closeModal();
    // currentPosition = 0;
    // mistakes = 0;
    // speed = 0;
    // velocity = [];
    location.reload() 
})